package customelement;

import customelement.abstraction.Element;
import org.openqa.selenium.WebElement;

public class LetterItem extends Element {

    public LetterItem(WebElement webElement) {
        super(webElement);
    }
}
